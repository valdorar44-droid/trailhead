---
import Layout from "../layouts/Layout.astro";
import { posts } from "../data/posts";
---

<Layout title="Trailhead Journal" description="Trailhead notes on maps, routes, places, and outdoor trip planning.">
  <main class="th-journal">
    <header>
      <p class="th-eyebrow">Journal</p>
      <h1>Field notes for the road.</h1>
      <p>Short notes on maps, routes, places, and how Trailhead should feel in use.</p>
    </header>
    <section class="th-post-list">
      {posts.map(post => (
        <a class="th-glass th-post-card" href={`/blog/${post.slug}`}>
          <span>{post.category}</span>
          <h2>{post.title}</h2>
          <p>{post.description}</p>
        </a>
      ))}
    </section>
  </main>
</Layout>
